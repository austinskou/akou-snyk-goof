name: Snyk Security

on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

permissions:
  contents: read

jobs:
  snyk:
    name: Snyk Code + Open Source Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read

    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # Optional: Set up Node.js if needed
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 20

      - name: Set up Snyk CLI
        uses: snyk/actions/setup@806182742461562b67788a64410098c9d9b96adb

      - name: Debug Snyk CLI version
        run: snyk --version

      - name: Debug Snyk token usage
        run: snyk test --dry-run
        continue-on-error: true

      - name: Snyk Code test
        run: snyk code test --sarif-file-output=snyk-code.sarif --severity-threshold=medium
        continue-on-error: true

      - name: Snyk Open Source monitor
        run: snyk monitor --all-projects
        continue-on-error: true

      - name: Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk-code.sarif
